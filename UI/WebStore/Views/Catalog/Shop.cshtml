@model CatalogViewModel

@{
    ViewData["Title"] = "Магазин";
    ViewBag.PageAction = "Shop";
}

<vc:bread-crumbs />

<partial name="Partial/_Advertisement" />

<section>
    <div class="container">
        <div class="row">

            <div class="col-sm-3">
                <partial name="Partial/_LeftSideBar" />
            </div>

            <div class="col-sm-9 padding-right">
                <partial name="Partial/_FeaturesItems" model="@Model" />
                @*@await Html.PartialAsync("Partial/_FeaturesItems", Model.Products)*@
            </div>
        </div>
    </div>
</section>
