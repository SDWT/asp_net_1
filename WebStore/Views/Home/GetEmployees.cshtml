@model IEnumerable<WebStore.ViewModels.EmployeeView>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>GetEmployees</title>
</head>
<body>
    <header>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/home/readconfig">Config</a></li>
            <li><a href="/TestPage.html">TestPage</a></li>
            <li><a href="/home/getemployees">Employees</a></li>
        </ul>
    </header>
    <content>
        <h3>@ViewBag.SomeData</h3>
        <h4>@ViewData["Test"]</h4>
        <table>
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Фамилия</th>
                    <th>Имя</th>
                    <th>Отчество</th>
                    <th>Возраст</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var employee in Model)
                {
                    <tr>
                        <td>@employee.FirstName</td>
                        <td>@employee.SecondName</td>
                        <td>@employee.Patronymic</td>
                        <td>@employee.Age</td>
                    </tr>
                }
            </tbody>
        </table>

        <hr />

        <ul>
            @foreach (var employee in Model)
            {
                <li>@Html.ActionLink(employee.SecondName, "EmployeeDetails", new { id = employee.Id })</li>
            }
        </ul>
    </content>
    <footer>
    </footer>
</body>
</html>
